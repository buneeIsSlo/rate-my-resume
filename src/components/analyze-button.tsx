"use client";

import { Button } from "./ui/button";

export default function AnalyzeButton() {
  return (
    <div>
      <Button className="mt-2 w-full cursor-pointer">Analyze resume</Button>
    </div>
  );
}
